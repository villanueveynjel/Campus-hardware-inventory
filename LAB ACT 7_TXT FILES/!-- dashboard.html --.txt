!--dashboard.html--!

{% extends 'base.html' %}{% block title %}Dashboard{% endblock %}
{% block content %}<div class="page-head"><div><h1>{{ 'Admin Dashboard' if session.get('role')=='admin' else 'User Dashboard' }}</h1><p>Campus Hardware Inventory System</p></div></div>
<div class="cards">
<div class="stat"><span>Total Stocks</span><strong>{{ total_stocks }}</strong><small>Total quantity of inventory stocks</small></div>
<div class="stat"><span>Account Role</span><strong>{{ session.get('role','user')|upper }}</strong><small>Current signed-in role</small></div>
<div class="stat"><span>Borrowed Items</span><strong>{{ borrowed_items }}</strong><small>{{ 'Across all users' if session.get('role')=='admin' else 'Your currently borrowed quantity' }}</small></div>
<div class="stat"><span>Pending Borrow Requests</span><strong>{{ pending_borrow }}</strong><small>Awaiting Admin approval</small></div>
<div class="stat"><span>Pending Returns</span><strong>{{ pending_returns }}</strong><small>{{ 'Admin approval panel' if session.get('role')=='admin' else 'Your pending return information' }}</small></div>
</div>
<section class="panel catalog-panel"><div class="panel-title row"><span>Hardware Catalog</span><span class="muted">Total Stocks: <strong>{{ total_stocks }}</strong></span></div>
<form class="filters" method="get"><input name="search" value="{{ search_query }}" placeholder="Search item or category"><select name="category"><option>All</option>{% for c in categories %}<option {% if category_filter==c %}selected{% endif %}>{{ c }}</option>{% endfor %}</select><button class="btn btn-primary">Search / Filter</button><a class="btn btn-secondary" href="{{ url_for('dashboard') }}">Clear</a><a class="btn btn-muted" href="{{ url_for('export_csv') }}">Export CSV</a></form>
{% if session.get('role')=='admin' %}<form class="inline-add" method="post" action="{{ url_for('add_inventory') }}"><input name="item_name" placeholder="New hardware item" required><select name="category">{% for c in categories %}<option>{{ c }}</option>{% endfor %}</select><input name="quantity" type="number" min="0" placeholder="Qty" required><button class="btn btn-success">+ Add Hardware</button></form>{% endif %}
<div class="table-wrap"><table><thead><tr><th>ID</th><th>Item</th><th>Category</th><th>Max Qty</th><th>Available Qty</th><th>Status</th>{% if session.get('role')=='admin' %}<th>Manage</th>{% endif %}</tr></thead><tbody>
{% for item in rows %}<tr><td>{{ item.item_id }}</td><td><strong>{{ item.item_name }}</strong></td><td>{{ item.category }}</td><td>{{ max_qty(item.category) }}</td><td>{{ item.quantity }}</td><td><span class="badge {{ item.status|lower|replace(' ','-') }}">{{ item.status }}</span></td>{% if session.get('role')=='admin' %}<td><form class="mini-form" method="post" action="{{ url_for('update_inventory',item_id=item.item_id) }}"><input type="number" min="0" max="{{ item.category and {'Microcontroller':20,'IC':30,'Motor':20,'Input Devices':20,'Output Devices':20,'Power Supply':20,'Communication':20,'Passive Components':50,'Tools':20,'Others':20}.get(item.category,20) }}" name="quantity" value="{{ item.quantity }}"><button class="btn btn-sm btn-primary">Update</button></form><form method="post" action="{{ url_for('delete_inventory',item_id=item.item_id) }}" onsubmit="return confirmAction('Delete this catalog record?')"><button class="btn btn-sm btn-danger">Delete</button></form></td>{% endif %}</tr>
{% else %}<tr><td colspan="7" class="empty">No hardware records match the current filter.</td></tr>{% endfor %}</tbody></table></div></section>
{% if session.get('role')!='admin' %}<section class="panel"><div class="panel-title row"><span>My Borrowed Items</span><a href="{{ url_for('requests') }}">View all requests</a></div><div class="table-wrap"><table><thead><tr><th>Item</th><th>Quantity</th><th>Approval</th><th>Return</th><th>Action</th></tr></thead><tbody>{% for r in borrowed_requests %}<tr><td>{{ r.hardware_item }}</td><td><strong>{{ r.quantity }}</strong></td><td>{{ r.approval_code }}</td><td>{{ r.return_status or 'Not Requested' }}</td><td>{% if r.return_status != 'Pending' %}<form method="post" action="{{ url_for('request_return',request_id=r.request_id) }}"><button class="btn btn-secondary btn-sm">Request Return</button></form>{% else %}<span class="badge pending">Return Pending</span>{% endif %}</td></tr>{% else %}<tr><td colspan="5" class="empty">No currently borrowed items.</td></tr>{% endfor %}</tbody></table></div></section>{% endif %}
{% endblock %}